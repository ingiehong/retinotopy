function dumcb(obj,event)

global masterlink

n=get(masterlink,'BytesAvailable')
if n > 0
    inString = fread(masterlink,n);
    inString = char(inString');
else
    return
end

inString;

inString = inString(1:end-1); 